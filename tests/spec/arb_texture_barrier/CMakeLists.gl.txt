include_directories(
	${GLEXT_INCLUDE_DIR}
	${OPENGL_INCLUDE_PATH}
)

link_libraries (
	piglitutil_${piglit_target_api}
	${OPENGL_gl_LIBRARY}
)

piglit_add_executable (arb_texture_barrier-blending-in-shader blending-in-shader-arb.c)
piglit_add_executable (arb_texture_barrier-texture-halves-ping-pong-operation-chain texture-halves-ping-pong-operation-chain.c)

# vim: ft=cmake:
